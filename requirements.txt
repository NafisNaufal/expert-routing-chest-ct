# ──────────────────────────────────────────────────────────────────────────────
# Pinned dependency set for: 1x NVIDIA A100 80GB, driver 470.x, CUDA 11.6 host.
#
# IMPORTANT: this driver only supports the cu118 PyTorch build.
#            cu12x wheels require driver >= 525 and will NOT run here.
#
# Do NOT run `pip install -r requirements.txt` directly — torch must come from
# the cu118 index and flash-attn from a prebuilt wheel. Use setup.sh, which
# installs things in the correct order. This file is the reproducibility record.
#
# Versions are pinned to match thirdparty/VILA/pyproject.toml so VILA's `llava`
# package (installed with --no-deps) runs against a coherent stack.
# ──────────────────────────────────────────────────────────────────────────────

# --- CUDA-sensitive: installed by setup.sh from the cu118 index/wheels ---
# torch==2.3.0+cu118          (https://download.pytorch.org/whl/cu118)
# torchvision==0.18.0+cu118   (https://download.pytorch.org/whl/cu118)
# flash-attn==2.5.8           (prebuilt cu118torch2.3 wheel — see setup.sh)

# --- VILA / llava core (must match thirdparty/VILA/pyproject.toml) ---
transformers==4.37.2
tokenizers==0.15.2
sentencepiece==0.1.99
accelerate==0.27.2
peft==0.10.0
bitsandbytes==0.43.1
einops==0.6.1
einops-exts==0.0.4
timm==0.9.12
shortuuid
# deepspeed: imported at llava module-load time (sequence_parallel). We never
# run deepspeed ops, so nothing is compiled — only the pure-Python comm module.
deepspeed==0.14.4
# s2wrapper: required by llava's vision encoder (VILA git dependency).
s2wrapper @ git+https://github.com/bfshi/scaling_on_scales
huggingface_hub==0.20.3
hf_transfer>=0.1.6        # fast multi-connection downloads for CT-RATE streaming
datasets==2.16.1

# --- numerics (numpy must stay <2 for transformers 4.37.2) ---
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
scikit-learn==1.2.2
scikit-image==0.22.0

# --- medical imaging ---
monai==1.4.0             # 1.4+ required for the VISTA3D network (vista3d132)
pytorch-ignite==0.4.11   # MONAI engine dependency — VISTA3D bundle uses Evaluator
nibabel==5.2.1
pydicom==2.4.4
pylidc==0.2.3
SimpleITK==2.3.1
tcia_utils>=2.0.0          # LIDC-IDRI download from TCIA

# --- retrieval ---
faiss-cpu==1.8.0
open_clip_torch>=2.24.0     # loads BiomedCLIP (hf-hub:microsoft/...) in the
                            # hybrid-framework retrieval evaluation

# --- utilities ---
setuptools<81            # pylidc imports pkg_resources, removed in setuptools 81+
tqdm==4.66.4
pyyaml==6.0.1
Pillow==10.3.0
matplotlib==3.8.4
umap-learn==0.5.5            # for the embedding-space UMAP figure
